[project]
name = "personal-app-frontend"
version = "1.0.2"
description = "This is the frontend of the personal app."
requires-python = ">=3.14,<3.15"
dependencies = [
  "nicegui==3.12.0",
  "loguru==0.7.3",
  "pydantic>=2.13.4,<2.16",
  "pydantic-settings>=2.12.0",
]


[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
